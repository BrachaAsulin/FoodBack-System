<UserControl x:Class="MonitoringLifestyle.Views.SearchFoodUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MonitoringLifestyle.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"             
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="700">
    <Grid Background="White">
        <Border CornerRadius="5" BorderThickness="3" BorderBrush="#FF727686" >
            <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="9*"/>
        </Grid.ColumnDefinitions>
        <materialDesign:PackIcon Grid.Column="0" Kind="Search" VerticalAlignment="Stretch" HorizontalAlignment="Center"  Height="48" Width="27" />
        <ComboBox x:Name="combo" Grid.Column="1" IsDropDownOpen="{Binding IsOpen}" ItemsSource="{Binding Foods}" SelectedValue="{Binding SelectedFoodName}" Text="{Binding FoodToSearch}" IsEditable="True" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"  />
            </Grid>
        </Border>
    </Grid>
</UserControl>
