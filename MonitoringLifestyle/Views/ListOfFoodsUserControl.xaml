<UserControl x:Class="MonitoringLifestyle.Views.ListOfFoodsUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MonitoringLifestyle.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="1090">
    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="0.5*"/>
            <ColumnDefinition Width="5*"/>
        </Grid.ColumnDefinitions>
        <Button Grid.Row="1" Grid.Column="1" Command="{Binding AddFoodButtton}" FontFamily="Arial" Content="Add Food" FontSize="16" FontWeight="Bold" />
        <ListBox x:Name="FoodsListBox" Grid.Row="1" Grid.Column="2"/>
        <local:SearchFoodUserControl Grid.Column="0" VerticalAlignment="Top" Height="100"/>


    </Grid>
</UserControl>
